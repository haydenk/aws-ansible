name: AWS Ansible Testing

on:
  push:
    branches:
      - master

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Setup python
      uses: actions/setup-python@v1
      with:
        python-version: 3.x
        architecture: x64
    - uses: actions/aws
    - name: Run
      run: python --version